[
  {
    "id": "visible-focus",
    "claim": "Adds a large high-contrast focus rail.",
    "where": "landing preview and extension panel",
    "test": "npm test -- --grep @claim:visible-focus",
    "sandbox": "fresh browser context at /demo; focus the sample search control and inspect its rail"
  },
  {
    "id": "reading-lane",
    "claim": "Shows and hides a temporary reading lane.",
    "where": "landing page and demo",
    "test": "npm test -- --grep @claim:reading-lane",
    "sandbox": "fresh browser context at /demo; toggle the reading lane"
  },
  {
    "id": "site-view",
    "claim": "Saves zoom and contrast settings for each site.",
    "where": "landing page, extension panel, and README",
    "test": "npm test -- --grep @claim:site-view",
    "sandbox": "fresh browser context at /demo; change zoom and contrast, reload, and inspect the demo namespace"
  },
  {
    "id": "named-waypoints",
    "claim": "Saves and opens named keyboard waypoints.",
    "where": "landing page, demo, and README",
    "test": "npm test -- --grep @claim:named-waypoints",
    "sandbox": "fresh browser context at /demo; create a waypoint and use it to focus the sample control"
  },
  {
    "id": "shortcut-export",
    "claim": "Exports the shortcut sheet as a text file.",
    "where": "extension panel, demo, and README",
    "test": "npm test -- --grep @claim:shortcut-export",
    "sandbox": "fresh browser context at /demo; download and inspect the text file"
  },
  {
    "id": "local-private",
    "claim": "Demo settings stay in an isolated browser-storage key and Reset demo removes that key.",
    "where": "landing page, privacy page, and README",
    "test": "npm test -- --grep @claim:local-private",
    "sandbox": "fresh browser context at /demo; save a setting, inspect the demo namespace, then reset it"
  },
  {
    "id": "no-egress",
    "claim": "Focus Lens sends no data: after its local page loads, its controls make no network requests.",
    "where": "privacy page and README",
    "test": "npm test -- --grep @claim:no-egress",
    "sandbox": "fresh browser context at /demo; record every request after load while changing settings, saving a waypoint, and exporting shortcuts"
  },
  {
    "id": "no-page-text-capture",
    "claim": "Waypoints store the name you enter and a control selector, never page text.",
    "where": "landing privacy section, privacy page, and README",
    "test": "npm test -- --grep @claim:no-page-text-capture",
    "sandbox": "fresh browser context at /demo; place a unique marker in sample page text, save a differently named waypoint, and inspect stored data"
  },
  {
    "id": "exact-permissions",
    "claim": "The packaged extension requests only activeTab, scripting, and storage permissions.",
    "where": "README",
    "test": "npm test -- --grep @claim:exact-permissions",
    "sandbox": "fresh browser context at /demo; inspect the manifest in the production extension directory and downloadable ZIP"
  },
  {
    "id": "no-account-free",
    "claim": "Works without an account and core features are free.",
    "where": "landing first screen and README",
    "test": "npm test -- --grep @claim:no-account-free",
    "sandbox": "fresh browser context at /demo; verify the complete controls have no account gate and the extension downloads"
  }
]
